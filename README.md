A simple integration of rust programs in org babel.

Source files are generated from the org files in the org/ subdirectory. I also used these to journal my progress, expect a lot of french in there.
